server {
  listen 8080;
  root /usr/share/nginx/html;
  index index.html;

  # BrowserRouter (react-router-dom) needs every path to fall back to index.html so a
  # direct load/refresh of e.g. /sessions/abc doesn't 404 at the server.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
